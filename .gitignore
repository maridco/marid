*.rbc
capybara-*.html
.rspec
/log
vendor/cache
/tmp
/db/*.sqlite3
/public/system
/coverage/
/spec/tmp
**.orig
rerun.txt
pickle-email-*.html
config/initializers/secret_token.rb
config/secrets.yml

## Environment normalisation:
/.bundle
/vendor/bundle

# these should all be checked in to normalise the environment:
# Gemfile.lock, .ruby-version, .ruby-gemset

# ignore DB config to allow local changes and automate deployment
database.yml
cequel.yml

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
.rvmrc

.DS_*
/log/*
log/*
