<%= form_for(@logup) do |f| %>
  <% if @logup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@logup.errors.count, "error") %> prohibited this logup from being saved:</h2>

      <ul>
      <% @logup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fullname %><br>
    <%= f.text_field :fullname %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
